# Archive

This folder keeps historical or imported project material that should remain available but should not dominate the repository root.

- `grain-grade-md-files/` - original exported planning and prompt material. Much of it duplicates the canonical `docs/` tree, so treat it as reference/archive content.
